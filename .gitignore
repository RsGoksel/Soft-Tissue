# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Model checkpoints (too large for git; reproducible via scripts)
*.pt
*.pth
*.ckpt

# Datasets (OpenBreastUS + generated HDF5; reproducible via scripts)
# Entire data/ directory excluded (logs + HDF5 + phantoms)
data/
*.h5
*.hdf5
*.mat
*.DAT
*.dat
*.csv

# Large source files kept out of the project root
Onaylanmayan*
breast_*.mat
dataset-*
hifu_*.mat
hifu_*.csv
*.crdownload

# OA-Breast raw downloads
*.crdownload
dataset-*.zip

# Deliverable bundle (regenerated by scripts/build_sent_bundle.py)
sent/
sent.zip

# Training logs and raw numpy dumps
outputs/**/train.log
outputs/**/*_train.log
outputs/bundle_rebuild.log
outputs/**/*.npz

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Personal notes / scratch (excluded from repo)
Claud.txt
system_map.md
Developments.md

# Abstract sources kept local for rebuild; repo only ships the PDFs
reports/abstract_*.md
